=======================================================
TURNKEY CRM — AI CONTEXT FILE
=======================================================
Last Updated: 03 July 2026
Current Session Working On: Vendor Payment Workflow (COMPLETE)
Next Session Should Start With: Admin Feature Control Panel (Phase 2)
=======================================================

PROJECT OVERVIEW:
- App Name: TurnkeyCRM
- Type: Construction/Real Estate CRM
- Live URL: https://teamtichyd-star.github.io/TurnkeyCRM/
- GitHub: https://github.com/teamtichyd-star/TurnkeyCRM
- Local Path: /Users/haritalla/Desktop/TIC/CRMLocal/TurnkeyCRM
- Hosting: GitHub Pages (static — NO server-side code)
- Status: LIVE APP WITH REAL DATA — Be careful with changes

TECH STACK:
- Frontend: Vanilla HTML, CSS, JavaScript (NO React, NO Vue)
- Database: Firebase Firestore
- Auth: Firebase Authentication
- Notifications: In-app (Firestore) + Web Push (browser native)
- PWA: Yes (manifest.json + sw.js)
- CSS: newstyle.css (ONE shared stylesheet)

FIREBASE:
- Project ID: turnkeycrm-ce7b8
- App ID: 9bc49e74-801f-4ffb-ab7e-ea78ba2f1089

PAGES IN THE APP:
1. index.html            -> Dashboard / Login + Parent shell (bell lives here)
2. leads.html            -> Lead Management
3. projects.html         -> Project Tracking + Payments + Snag List + Expenditure
4. tasks.html            -> Task Management
5. followups.html        -> Follow-up Tracker
6. sheets.html           -> Sheets/Reports
7. export.html           -> Data Export
8. admin-panel.html      -> Admin Controls
9. client-view.html      -> Client Portal (Snag List + Payments)
10. vendors.html         -> Vendor Management
11. attendance.html      -> Attendance (mark own)
12. attendance-admin.html -> Attendance Admin (all users + CSV)
13. staff-profile.html   -> Staff Profile Form
14. staff-profile-admin.html -> Staff Directory (admin only)

KEY JS FILES:
- js/notifications.js -> TIC_NOTIF engine (in-app + web push)
- js/userManagement.js

RULES FOR ALL AIs — FOLLOW STRICTLY:
1. Vanilla JS ONLY — No React, Vue or any framework
2. GitHub Pages — No server-side code or Node.js
3. No new npm packages — there is no package.json
4. No new CSS files — use newstyle.css only
5. No style tags inside HTML — use newstyle.css classes
6. Do NOT duplicate Firebase initialization
7. Always check existing function names before creating new ones
8. Match camelCase naming for all functions
9. Always provide COMPLETE file content not partial snippets
10. Do NOT change any Firestore collection or document names
11. Do NOT change image or icon paths
12. Images and icons are in ROOT folder — do not move them
13. ALWAYS backup before changes: git add . && git commit -m "save point" && git push

USER ROLES:
- superadmin, admin, manager, designer, supervisor, accounts, projectmanager
- Designer can see ALL projects
- Supervisor sees only assigned

FIRESTORE COLLECTIONS (DO NOT RENAME):
- users               -> name, email, role, notifications[]
- leads               -> name, phone, status, assignedTo
- projects            -> name, client, manager, snags[], updates[], jobWorks[]
- tasks               -> title, assignedTo, status, comments[]
- followups           -> name, date, assignedTo
- notifications       -> to, toName, title, body, type, link, read, createdAt
- vendors             -> name, phone, workType, gst, pan, upi
- attendance          -> userEmail, userName, date, time, type, gpsLat, gpsLng, gpsAddress, projectId, projectName, notes, createdAt
- staffProfiles       -> employeeId, fullName, email, mobile1/2/3, address, emergency, aadhar/pan, education, work, bank, declarationAccepted, profileStatus, driveFolderId

=======================================================
VENDOR PAYMENT WORKFLOW — COMPLETE (v18.3)
=======================================================

WHAT WAS BUILT (Session 03 July 2026):

1. CATEGORY FIELD on Job Works
   - "In Quote" (green badge) or "Additional Work" (orange badge)
   - Shows on job work card
   - editJobWork() loads category correctly

2. REQUEST PAYMENT BUTTON on job work cards
   - Blue money-bill-wave button
   - Visible to: admin, superadmin, manager, supervisor, accounts
   - Opens Payment Request modal

3. PAYMENT REQUEST MODAL
   - Fields: Scope (Within Scope / Additional Work), Amount, Reason, Notes
   - Job Work name + Category shown read-only
   - Hard block: cannot exceed available balance
   - Saves to jw.paymentRequests[] with status='pending'
   - Notifies all admins + accounts via bell

4. PAY REQUESTS SUB-TAB (3rd sub-tab in Expenditure)
   - Filter buttons: All / Pending / Paid / Rejected
   - Pending badge count on tab button
   - Request cards show: amount, job work, category, scope, requester, date, reason, status
   - Paid cards show: payment mode, TXN ID, date, accountant notes
   - Rejected cards show: rejection reason

5. MARK PAID MODAL (accounts/admin)
   - Fields: Payment Mode, TXN ID, Date, Notes
   - Auto-logs to jw.payments[] for tracking
   - Notifies requester + admins

6. REJECT (accounts/admin)
   - Prompt for rejection reason
   - Notifies requester with reason

7. ADMIN EDIT/DELETE
   - Edit pending request: change amount, scope, reason
   - Edit paid request: change payment details
   - Delete any request (removes from payments log if paid)
   - Visible to: admin, superadmin ONLY

8. ADDITIONAL WORKS SUMMARY in Expenditure
   - Summary card shows: Total Contract / Total Paid / Balance (top)
   - Additional Works breakdown below: Contract / Paid / Pending (orange)
   - Calculated from jw.category === 'Additional Work'

DATA STRUCTURES:

Job Work object:
{
  id, workType, vendorId, vendorName,
  contractValue,
  category: "In Quote" | "Additional Work",
  status: "active",
  notes,
  payments: [ {amount, mode, date, note, paidBy, createdAt} ],
  paymentRequests: [ ...see below... ]
}

Payment Request object:
{
  id: "pr_" + timestamp,
  amount, reason, notes,
  scope: "Within Scope" | "Additional Work",
  category: "In Quote" | "Additional Work",
  jobWorkName,
  requestedBy, requestedByEmail, requestedAt,
  status: "pending" | "paid" | "rejected",
  paidBy, paidByEmail, paidAt,
  transactionId, paymentMode, paymentDate, accountantNotes,
  rejectedReason, rejectedBy, rejectedAt,
  lastEditedBy, lastEditedAt
}

ROLE PERMISSIONS (Payment Workflow):
- Request Payment:  admin, superadmin, manager, supervisor, accounts
- Mark Paid:        admin, superadmin, accounts
- Reject:           admin, superadmin, accounts
- Edit Request:     admin, superadmin ONLY
- Delete Request:   admin, superadmin ONLY
- Edit Payment:     admin, superadmin ONLY

KEY GOTCHAS LEARNED:
1. Role variable in projects.html is 'currentUserRole' (local var from URL param)
   NOT window.currentUserRole — always use: currentUserRole || window.currentUserRole
2. HTML sub-sections added to SPA must be verified with grep after Python write
3. switchExpSubTab must use null-safe checks for all section divs

=======================================================
GIT TAGS (RESTORE POINTS):
=======================================================
- v18.3-vendor-payment-complete = CURRENT STABLE (full payment workflow)
- v18.2-vendor-payment-requests = payment requests tab
- v18.1-vendor-payment-wip = category field only
- v18-tasks-comments-mobile-notifications = before vendor payment work
- v17-staff-profile-complete = staff profile module
- v16-stable-drive-integration = drive integration
- v15-stable-permissions-stage3-complete = permissions
- v12-permissions-stage2 = permissions stage 2
- v10-attendance-module-live = attendance module
- v9-expenditure-mobile-ux = expenditure + mobile UX
- v8-vendors-module-live = vendors module
- v7-firestore-rules-locked = firestore rules
- v6-zapier-notifications-live = zapier notifications

=======================================================
PENDING NEXT SESSION (in priority order):
=======================================================

1. 🔴 ADMIN FEATURE CONTROL PANEL (Phase 2)
   - Unified permission system per user
   - Control who can: request payment, mark paid, edit, delete
   - Merge with existing permissions stage 3
   - Already agreed: build after payment workflow complete

2. 🟡 CASH FLOW ENHANCEMENTS
   - Show pending payment requests amount in summary
   - (Paid + Pending Requests + True Balance)

3. 🟡 SNAG -> TASK SYNC
   - When snag added -> auto-create task
   - Two-way status sync
   - SNAG badge on task card

4. 🟢 UI POLISH (carryover)
   - WhatsApp button green (#25D366)
   - Email button blue (#4A90E2)
   - GST breakdown in client portal payments

5. 🟢 NICE-TO-HAVE
   - Expense Tracking module (project P&L)
   - Invoice Generator (PDF)
   - PWA Install Prompt banner
   - Global Search
   - Auto-mark Sundays as Weekly Off

=======================================================
GIT WORKFLOW:
=======================================================
SAVE WORK:
  git add . && git commit -m "description" && git push origin main

UNDO UNSAVED CHANGES:
  git reset --hard HEAD

ROLLBACK LAST COMMIT:
  git reset --hard HEAD~1 && git push origin main --force

RESTORE STABLE POINT:
  git checkout v18.3-vendor-payment-complete
