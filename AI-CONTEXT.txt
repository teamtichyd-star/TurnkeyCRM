=======================================================
TURNKEY CRM — AI CONTEXT FILE
=======================================================
Last Updated: 01 June 2026
Current Session Working On: Google Ads -> CRM Lead Integration (IN PROGRESS)
Next Session Should Start With: Test push reliability on mobile
=======================================================

PROJECT OVERVIEW:
- App Name: TurnkeyCRM
- Type: Construction/Real Estate CRM
- Live URL: https://teamtichyd-star.github.io/TurnkeyCRM/
- GitHub: https://github.com/teamtichyd-star/TurnkeyCRM
- Local Path: /Users/haritalla/Desktop/TIC/CRMLocal/TurnkeyCRM
- Hosting: GitHub Pages (static — NO server-side code)
- Status: LIVE APP WITH REAL DATA — Be careful with changes

TECH STACK:
- Frontend: Vanilla HTML, CSS, JavaScript (NO React, NO Vue)
- Database: Firebase Firestore
- Auth: Firebase Authentication
- Notifications: In-app (Firestore) + Web Push (browser native)
- PWA: Yes (manifest.json + sw.js)
- CSS: newstyle.css (ONE shared stylesheet)

FIREBASE:
- Project ID: turnkeycrm-ce7b8
- App ID: 9bc49e74-801f-4ffb-ab7e-ea78ba2f1089

PAGES IN THE APP:
1. index.html       → Dashboard / Login + Parent shell (bell lives here)
2. leads.html       → Lead Management
3. projects.html    → Project Tracking + Payments + Snag List
4. tasks.html       → Task Management
5. followups.html   → Follow-up Tracker
6. sheets.html      → Sheets/Reports
7. export.html      → Data Export
8. admin-panel.html → Admin Controls
9. client-view.html → Client Portal (Snag List + Payments)

KEY JS FILES:
- js/notifications.js → TIC_NOTIF engine (in-app + web push)
- js/userManagement.js

RULES FOR ALL AIs — FOLLOW STRICTLY:
1. Vanilla JS ONLY — No React, Vue or any framework
2. GitHub Pages — No server-side code or Node.js
3. No new npm packages — there is no package.json
4. No new CSS files — use newstyle.css only
5. No style tags inside HTML — use newstyle.css classes
6. Do NOT duplicate Firebase initialization
7. Always check existing function names before creating new ones
8. Match camelCase naming for all functions
9. Always provide COMPLETE file content not partial snippets
10. Do NOT change any Firestore collection or document names
11. Do NOT change image or icon paths
12. Images and icons are in ROOT folder — do not move them
13. ALWAYS backup before changes: git add . && git commit -m "save point" && git push

USER ROLES:
- superadmin, admin, manager, designer, supervisor, accounts, projectmanager
- Designer can see ALL projects
- Supervisor sees only assigned

CURRENT MODULE STATUS:
- Dashboard:      complete
- Leads:          complete (notifications added)
- Projects:       complete (notifications added)
- Tasks:          complete (notifications added)
- Follow-ups:     complete (notifications added)
- Sheets:         complete
- Export:         complete
- Admin Panel:    complete
- Client Portal:  complete (snag notifications added)
- Notifications:  complete (in-app + web push; mobile shows white silhouette in status bar - Android PWA limitation, not bug)

FIRESTORE COLLECTIONS (DO NOT RENAME):
- users               → name, email, role, notifications[]
- leads               → name, phone, status, assignedTo
- projects            → name, client, manager, snags[], updates[]
- tasks               → title, assignedTo, status
- followups           → name, date, assignedTo
- notifications       → to, toName, title, body, type, link, read, createdAt

=======================================================
NOTIFICATION SYSTEM (FULLY DOCUMENTED)
=======================================================

✅ HOW IT WORKS:
- Call TIC_NOTIF.send(db, userName, message, type, link)
- Auto-resolves userName → email via users collection
- Writes to 'notifications' collection
- Bell badge updates real-time (onSnapshot listener)
- OS notification fires if Notification.permission granted

📍 WHERE TIC_NOTIF.send IS CALLED:
- projects.html (4 places): new project, update, manager assign, snag
- tasks.html (2 places): new + update
- leads.html (2 places): new + update
- followups.html (2 places): new + update
- client-view.html (1 place): client adds snag → notify project manager

🔔 BELL HTML LOCATION:
- File: index.html (parent shell, NOT in iframes)
- Element IDs: notifBell, bellIcon, notifCount
- Click handler: showNotifications()
- Real-time listener: listenNotifications(userEmail) using onSnapshot
- Variable: window._notifUnsub (unsubscribe handle)

🎯 WEB PUSH (OS NOTIFICATIONS):
- Permission button: enablePushNotifications() in index.html
- Uses service worker: navigator.serviceWorker.ready.showNotification()
- Service worker file: sw.js (handles notification clicks)
- Icon URL: https://teamtichyd-star.github.io/TurnkeyCRM/icon-192.png
- Sound + vibration: vibrate: [300, 100, 300, 100, 300]

📌 RESTORE POINTS (git tags):
- v1-notifications-complete = stable in-app only
- v2-webpush-stable = current (web push added)
- working-notifications-v1 = early stable
- stable-no-bell = before any bell work

KNOWN ISSUES:
- Web push fires intermittently on mobile (works sometimes, not always)
- Suspected: service worker cache, OS battery saver, PWA not installed
- iOS requires PWA install + iOS 16.4+
- "Fetch event handler no-op" warnings = harmless service worker warning
- OneSignal old code disabled (commented out) but still in files

=======================================================
PENDING UI FIXES:
=======================================================
- Share tab: WhatsApp button should be green, Email button should be blue
- Client portal: GST breakdown missing in payments tab
- Share tab: WhatsApp button green styling not applying

=======================================================
COMPLETED IN PREVIOUS SESSIONS:
=======================================================
- Fixed projects page (try-catch syntax error)
- Designer role can access all projects
- Added Snag List + Payments tabs to client portal
- Removed 4 duplicate sendSnagNotification functions
- Built complete notification system (in-app + web push)
- Real-time bell updates via onSnapshot
- Auto mark-read on bell click
- Logo on push notifications

=======================================================
TODO / NEXT SESSION:
=======================================================

🔴 PRIORITY - DEBUG NOTIFICATIONS:
- [ ] Test why mobile push fails sometimes
- [ ] Verify service worker properly registered on each device
- [ ] Add console logs to track notification flow
- [ ] Test with PWA installed vs browser tab
- [ ] Check phone battery optimization settings
- [ ] Document working/non-working scenarios

🟡 PENDING UI:
- [ ] WhatsApp button green (share tab)
- [ ] Email button blue (share tab)
- [ ] GST breakdown in client portal payments
- [ ] Notification action buttons (Mark Done, Open)

🟢 FUTURE (LARGER WORK):
- [ ] Real WhatsApp-like push when app closed
  → Requires Firebase Cloud Functions (Blaze plan)
  → Free tier sufficient for normal usage
  → Requires credit card on file
- [ ] Capacitor wrap → Play Store deployment
- [ ] Native FCM for true background notifications

=======================================================
GIT WORKFLOW:
=======================================================
SAVE WORK:
  git add . && git commit -m "description" && git push origin main

UNDO UNSAVED CHANGES:
  git reset --hard HEAD

ROLLBACK LAST COMMIT:
  git reset --hard HEAD~1 && git push origin main --force

RESTORE STABLE POINT:
  git checkout v2-webpush-stable

TERMINAL SHORTCUT (set once):
  echo 'alias crm="cd ~/Desktop/TIC/CRMLocal/TurnkeyCRM"' >> ~/.zshrc
  source ~/.zshrc
  Then just type 'crm' from anywhere

=======================================================
DEBUGGING TIPS:
=======================================================
- Bell not updating? → Check listenNotifications() called on login
- Notification not sending? → Check Firestore 'notifications' collection
- Web push not firing? → Check Notification.permission === 'granted'
- Service worker cached? → Chrome → site settings → clear & reset
- Check console for: "✅ Notification sent to..." (confirms TIC_NOTIF works)

=======================================================


=======================================================
SESSION UPDATE - 01 June 2026
=======================================================

WORKING ON: Google Ads Lead Form -> CRM Auto-Integration

PROGRESS:
- Decided: Zapier (Option E) - extend existing Google Ads -> Telegram zap
- Added Firebase Firestore action to Zap (3rd step)
- Test sent record to Firestore successfully
- BUT broke leads page (missing fields error)
- Discovered 90+ existing leads also missing 'assignedTo' field
- FIXED: Added empty defaults to all existing leads
- DELETED: Bad Zapier test lead (QhXIsDZrq8ZnK7zsMpue)
- Leads page working again

WHERE TO RESUME:
- Zapier Firebase action is configured but NOT published
- Need to: properly map ALL required fields in Zapier
- Need to: test again with safe defaults
- Need to: add notification trigger when lead created via Zapier

REQUIRED LEAD FIELDS (from leads.html saveLead function):
- name (required)
- phone (required)
- email
- company
- projectType
- location
- budget
- source
- status (default: 'New')
- followupDate
- followupNote
- assignedTo (default: 'Mustaq')
- area
- notes
- date
- createdAt
- updatedAt

ASSIGNMENT RULE:
- All Google Ads leads -> assignedTo: "Mustaq" (manager role)
- Mustaq email: admin@sbinteriorsolutions.net

LEAD SOURCE FORMAT FROM TELEGRAM:
- Name + Last Name -> combine into 'name'
- Phone -> phone (strip country code if present)
- Email -> email
- Requirements -> notes
- Form Name -> source as "Google Ads - [Form Name]"
- Submitted date -> date field

PENDING NEXT SESSION:
1. Re-configure Zapier Firebase action with ALL fields mapped
2. Use empty strings for blank fields (NOT undefined)
3. Test once - verify leads page works
4. Add: When Zapier creates lead -> also write to 'notifications' collection
   so Mustaq gets bell notification
5. Then publish Zap
6. Later: add notifications to other pages from PENDING UI section

CRITICAL LEARNING:
- Firestore documents from Zapier MUST have ALL fields with proper types
- Missing field = undefined = breaks .toLowerCase() in filterLeads
- Always send empty string '' instead of leaving field out

=======================================================

=======================================================
SESSION UPDATE - 03 June 2026
=======================================================

✅ COMPLETED: Google Ads -> Zapier -> CRM Integration LIVE!

ARCHITECTURE (FREE PLAN OPTIMIZED):
- Zap 1: "Tic leads to telegram" (existing)
    Google Ads -> Telegram alert (1 task)
- Zap 2: "Google Ads to CRM" (NEW, LIVE)
    Google Ads -> Firestore 'leads' collection (1 task)
- Total: 2 tasks per lead = 50 leads/month FREE

ZAPIER FIELD MAPPING (Zap 2):
- name: Full Name
- phone: Phone Number (raw, with +91)
- notes: Could You Tell Us More About Your Project
- source: "Google Ads" (hardcoded)
- status: "New" (hardcoded)
- assignedTo: "Mustaq" (hardcoded)
- email: (empty space - form has email but not required)
- 8 other empty fields skipped (CRM auto-fills)
- date/createdAt/updatedAt: skipped (CRM auto-fills)

CRM PATCH (leads.html loadLeads function):
- Strips +91 prefix from phone on display
- Auto-fills missing createdAt/date/updatedAt
- Defaults blank fields to empty strings (prevents .toLowerCase crashes)
- Defaults status to 'New', assignedTo to '' if missing
- Won't affect existing 90+ leads (they already have all fields)

KNOWN BEHAVIOR:
- Phone stored in Firestore WITH +91 (raw from Google Ads)
- Phone displayed in CRM WITHOUT +91 (normalized on load)
- WhatsApp links work correctly: https://wa.me/91{stripped_phone}
- Test lead "Adinath k Wadekar" exists in CRM (can delete)

GOOGLE ADS FORM FIELDS:
- Full Name (required)
- Phone Number (required, +91 format)
- Email (optional, NOT required by Google Ads rule)
- Could You Tell Us More About Your Project (free text)

ZAPIER FREE PLAN LIMITS:
- 100 tasks/month total
- 2-step Zaps only (3+ steps = Pro plan trigger)
- Formatter steps require Pro
- Triggers are FREE (don't count)

PENDING NEXT SESSION:
1. Add bell notification for Mustaq when Zapier lead arrives
   - Implementation: Firestore onSnapshot listener in leads.html
   - Detect new leads where source='Google Ads' AND assignedTo='Mustaq'
   - Call TIC_NOTIF.send() to write notification + trigger bell
   - FREE - runs in browser, no Zapier task used
2. Test web push reliability on mobile (carry over from previous session)
3. Pending UI fixes (WhatsApp green, Email blue buttons, GST breakdown)

RESTORE POINTS:
- v5-google-ads-integration-live = CURRENT STABLE
- v4-google-ads-integration-wip = before this session
- v3-notifications-final = before Google Ads work
- v2-webpush-stable = web push baseline
- v1-notifications-complete = in-app notif baseline


=======================================================
SESSION UPDATE - 03 June 2026 (Part 2)
=======================================================

✅ COMPLETED: Auto-Bell Notification for Google Ads Leads

IMPLEMENTATION:
- Added Firestore onSnapshot listener at bottom of leads.html
- Listener wrapped in <script> tags (was floating outside, didn't run)
- Uses zapierListenerReady flag (3s timeout) to skip initial page load
- Filters: lead.source === 'Google Ads' only
- Notifies BOTH:
    * Mustaq (manager)
    * admin@sbinteriorsolutions.net (main admin)
- Cost: ZERO Zapier tasks (runs in browser)

DEBUG LOGS IN CONSOLE:
- 📡 Firestore snapshot received, changes: N
- ➕ Added lead: NAME | source: X | armed: true/false
- 🔔 Zapier listener now armed (after 3s)
- ✅ New Google Ads lead detected! Sending notifications...

KNOWN LIMITATION (CARRY OVER):
- Mobile push notification favicon sometimes missing
- Android status bar shows white silhouette (OS-level, not bug)
- Documented as known issue, deferred for dedicated session

RESTORE POINTS:
- v6-zapier-notifications-live = CURRENT STABLE (auto-bell working)
- v5-google-ads-integration-live = before notifications
- v4-google-ads-integration-wip = before this session

PENDING NEXT SESSION:
1. Debug mobile push reliability (favicon + intermittent delivery)
   - Check service worker cache strategy
   - Verify icon URL works on mobile data
   - Test PWA install vs browser tab behavior
2. Pending UI fixes:
   - WhatsApp button green (share tab)
   - Email button blue (share tab)
   - GST breakdown in client portal payments
3. Consider: Add Telegram step removal if redundant (save 50 leads/month)

